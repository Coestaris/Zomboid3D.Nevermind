/*
 * Simple program example
 */

import systypes;
import io;

entrypoint function integer main()
{
    var a : uint = 5u + 1u;
    var b : short = 5s + 5us;
    var c : byte = 40o;

    //Compile error
    var v : byte = 1000o;
}