/*
 * Simple program example
 */

import systypes;
import io;

function real func(a : real)
{
     return a * 2;
}

entrypoint function integer main()
{
   var a : real = 0.0;
   if(func(a) == 0)
   {
       a = ~a;
   } 
}