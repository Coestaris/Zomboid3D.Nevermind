/*
 * Simple program example
 */

import systypes;
import io;

entrypoint function integer main()
{
    var a : integer = 4u;
    //a = 1 + (2 + 3);

    //a = 2 + (3 + 4);

    a = (1s + 2o) + (4s + 8);
}
