/*
 * Simple program example
 */

import systypes;
import io;

entrypoint function integer main()
{
    var tmp : integer = 0;
    var a : integer = 2 * (5 + 6);

    if(a == 5)
    {
        tmp = 0;
    }

    if(a != 5)
    {
        tmp = 1;
    }
}