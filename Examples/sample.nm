/*
 * Simple program example
 */

import systypes;
import io;

entrypoint function integer main()
{
    var tmp : integer = 0;
    var a : integer = 2 * (5 + 6);

    if(a == 5 || a == 6)
    {
        if(a == 5) { tmp = 2; }
        else { tmp = 3; } 
    } 
    else
    {
        tmp = 1;
    }
}