/*
 * Simple program example
 */

import systypes;
import io;

entrypoint function integer main()
{
    var a : integer = 0;
    //a = 1 + (2 + 3);

    //a = 2 + (3 + 4);

    a = (1 + -2) + (4 + 8);
}