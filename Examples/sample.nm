/*
 * Simple program example
 */

import systypes;
import io;

function integer func(a : integer, b : integer)
{
     return b + a * 2;
}

entrypoint function integer main()
{
   var a : integer = 4;
   if(func(a * 2, a / 2) == 52)
   {
       a = ~a;
   } 
}